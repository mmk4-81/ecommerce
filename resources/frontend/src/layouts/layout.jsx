import {Container, Row} from 'react-bootstrap';
import "../styles/style.module.sass";

const Layout = ()=>{
    return (
        <Container fluid>
            <Row>
                
            </Row>
        </Container>
    );
   }

   export default Layout;
